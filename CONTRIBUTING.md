# 手を入れるときに

Issue も pull request も歓迎する。**登録は要らない。**

## ここに何があるか

**凍結された論文の PDF と、その正誤表と、両者を突き合わせる検査である。**
新しい研究を進める場所ではない。**紙面が何を主張し、そのどこが誤っていたかを、
機械で確かめられる形に保つ場所である。**

## いちばん知らせてほしいもの

**紙面の誤りである。**[ERRATA.md](ERRATA.md) に載っていない誤りを見つけたら、
<https://github.com/cpsbvbng26-dotcom/trinity-infinity/issues> に立ててほしい。

1. どの論文の、どの頁・どの式か
2. 何が誤っているか
3. 正しくはどうか（分かる範囲で）

**定理と数値の誤りは、最優先で扱う。**

## PDF を直さない

**公開済みの PDF は改変も再生成もしない。**凍結された成果物である。
誤りが見つかったら、**直すのは PDF ではなく正誤表のほうである。**

これは方針であると同時に、検査が前提にしている性質でもある。
`verification/check_errata.py` は、正誤表の各項目が紙面の実際の文字列と対応するかを
当たっている。**紙面を書き換えれば、正誤表のほうが嘘になる。**

## 変更を出す

```
pip install -r requirements.txt
python3 verification/check_text.py          # 誤変換とバッジ記法
python3 verification/independent_check.py   # 定理
python3 verification/claims_audit.py        # 印字された数値
python3 verification/check_errata.py        # 正誤表の監査
python3 verification/check_route.py         # 経路
```

**通ってから出す。**乱数を使う検査は種を固定してある。

## 検査を足すときは、壊す試験も一緒に足す

**通ることでは信用できない。**通る状態を壊して、その検査がちょうど落ちることを
確かめる。**そして、名乗っている数を直す。**項目数はワークフローの仕事の名前と
README が名乗っており、ずれると落ちる。

## 通らない変更

- **検査の期待値や乱数種を、通すために変えること**
- **PDF の改変・再生成**
- **推測で埋めた DOI。**確かめていない番号は「確かめていない」と書く
- **正誤表の項目を消すこと。**直した項目も、直したと書いて残す

## 支えの範囲と、これからの見込み

- **維持しているのは著者一人である**（根本卓哉）。組織の後ろ盾は無い
- issue への返答は**数日から一週間**を目安とする。約束ではない
- **範囲。**この三篇の紙面と、その正誤表と、両者の突き合わせに限る。
  作用素そのものの新しい研究は [trinity-operator](https://github.com/cpsbvbng26-dotcom/trinity-operator) で扱う
- **紙面は増えない見込みである。**三篇は凍結してあり、新しい版を出す予定は無い

## ライセンス

散文と論文は [CC BY 4.0](LICENSE)、実装は [MIT](LICENSE-CODE)。
